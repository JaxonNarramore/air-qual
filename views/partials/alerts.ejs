<% if (alerts.error) { %>
    <% alerts.erorr.forEach(msg => { %>
        <div class="alert alert-error"><%= msg %> </div>
   <% }) %> 
<% } %> 

<% if (alerts.success) { %>
    <% alerts.success.forEach(msg => { %>
        <div class="alert alert-success"><%= msg %> </div>
   <% }) %> 
<% } %> 